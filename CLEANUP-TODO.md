# Code Cleanup TODO

Generated by `knip` dead code analysis on 2026-02-04.

Run `pnpm knip` to regenerate this report.

---

## Summary (Updated)

| Category | Original | Current | Action |
|----------|----------|---------|--------|
| Unused files | 60 | **40** | Review & delete |
| Unused dependencies | 31 | **30** | Review & remove |
| Unused devDependencies | 24 | 24 | Review & remove |
| Unused exports | 227 | ~200 | Remove or mark internal |

---

## Completed Actions

### Session 1 - Dependency Fixes
- [x] Removed `react-native-ble-plx` (not used, BLE example is disabled)
- [x] Added missing dependencies: `nanoid`, `@expo/vector-icons`, `expo-file-system`, `zod`, `quickjs-emscripten-core`, `@vitejs/plugin-react`, `webpack`, `nativewind`, `@storybook/react`, `glob`, `ws`
- [x] Configured knip to recognize `game-inspector-mcp` dynamic tool imports
- [x] Created root symlink for `tailwind.config.js` (knip workaround)

### Session 2 - File Deletions (20 files)

**RunPod scripts deleted (4 files):**
- [x] `api/scripts/test-runpod-api.ts`
- [x] `api/scripts/test-runpod-premade.ts`
- [x] `api/scripts/quick-runpod-test.sh`
- [x] `api/scripts/test-comfyui-api.ts`

**RunPod documentation deleted (3 files):**
- [x] `docs/plans/runpod-comfyui-setup-status.md`
- [x] `docs/plans/runpod-comfyui-rollout-plan.md`
- [x] `docs/plans/comfyui-migration-architecture.md`

**RunPod workflow deleted:**
- [x] `api/src/ai/workflows/txt2img-runpod.json`

**Button experiment scripts deleted (7 files):**
- [x] `api/scripts/test-button-generation.ts`
- [x] `api/scripts/test-button-shape-flux-fixed.ts`
- [x] `api/scripts/test-button-shape-preservation.ts`
- [x] `api/scripts/generate-button-matrix.ts`
- [x] `api/scripts/generate-button-controlnet.ts`
- [x] `api/scripts/generate-button-all-states.ts`
- [x] `api/scripts/tune-img2img-buttons.ts`

**Duplicate UI generation scripts deleted (3 files):**
- [x] `api/scripts/generate-ui-button.ts`
- [x] `api/scripts/generate-ui-checkbox.ts`
- [x] `api/scripts/generate-ui-textures.ts`

**Note:** Main UI CLI kept at `api/src/cli/generate-ui.ts` (invoked via `pnpm generate:ui`)

**Unreferenced test scripts deleted (8 files):**
- [x] `api/src/ai/__tests__/silhouette-img2img-test.ts`
- [x] `api/src/ai/__tests__/quick-img2img-test.ts`
- [x] `api/src/ai/__tests__/scenario-visual-test-runner.ts`
- [x] `api/src/ai/__tests__/generate-test-games.ts`
- [x] `api/src/ai/__tests__/parallax-layered-test.ts`
- [x] `api/scripts/test-controlnet-canny.ts`
- [x] `api/scripts/test-scenario-api.ts`
- [x] `api/scripts/test-ui-generation.ts`

---

## Decisions Made

### Keep for Now
- **AI Pipeline code** (`api/src/ai/pipeline/`) - User wants to understand before deleting
- **Seed scripts** (`seed-economy.ts`, `seed-system-user.ts`, `seed-themes.ts`) - May be needed for DB setup
- **quickjs-emscripten** - Will be used for scripting
- **react-native-purchases** - Will be used for IAP
- **expo-linking** - Keep for deep linking

### Consolidated
- UI generation: Multiple scripts → Single CLI at `api/src/cli/generate-ui.ts`

---

## 1. Remaining Unused Files (40)

### api/scripts/ (13 files) - Review individually

**Seed/Setup scripts (may be useful):**
```
api/scripts/seed-economy.ts
api/scripts/seed-system-user.ts
api/scripts/seed-themes.ts
api/scripts/sync-templates.ts
```

**Asset/experiment scripts (review):**
```
api/scripts/download-pack-assets.ts
api/scripts/generate-breakout-silhouettes.ts
api/scripts/manual-asset-test.ts
api/scripts/process-job.ts
api/scripts/regenerate-test-silhouettes.ts
api/scripts/restructure-local-assets.ts
api/scripts/theme-game.ts
api/scripts/ui-compare.ts
api/scripts/ui-experiment.ts
```

### api/src/ (6 files) - Review before deleting

**AI Pipeline (keep until understood):**
```
api/src/ai/pipeline/executor.ts
api/src/ai/pipeline/index.ts
api/src/ai/pipeline/registry.ts
api/src/ai/pipeline/stages/index.ts
api/src/ai/tiles.ts
```

**Test fixtures:**
```
api/src/__fixtures__/vcr/vcr-utils.ts
```

### shared/src/ (17 files) - Possibly dead features

**Bundle system:**
```
shared/src/bundle/index.ts
```

**Generators:**
```
shared/src/generator/angryBurns/index.ts
shared/src/generator/SeededRandom.demo.ts
shared/src/generator/slopeggle/__examples__/generated-levels.ts
shared/src/generator/slopeggle/index.ts
shared/src/generator/slopeggle/SlopeggleLevelGenerator.ts
shared/src/generator/slopeggle/types.ts
```

**Loader system:**
```
shared/src/loader/__examples__/usage.ts
shared/src/loader/index.ts
shared/src/loader/LevelLoader.ts
shared/src/loader/PackSource.ts
```

**Motion system:**
```
shared/src/motion/clustering.ts
shared/src/motion/index.ts
shared/src/motion/motion.example.ts
shared/src/motion/PegMotionAssigner.ts
```

**Scripting (EvalSandbox - may be replaced by QuickJS):**
```
shared/src/scripting/EvalSandbox.ts
shared/src/scripting/index.ts
```

### apps/storybook/ (3 files) - Expo mode files

```
apps/storybook/app/_layout.tsx
apps/storybook/app/index.tsx
apps/storybook/app/interaction-story.tsx
```
**Note**: May be needed for Storybook's Expo mode.

### packages/ui/ (1 file)

```
packages/ui/src/SortableList/index.verify.ts
```

---

## 2. Unused Dependencies (30)

### Keep (User Requested)
- `quickjs-emscripten` - Will be used for scripting
- `react-native-purchases` - Will be used for IAP  
- `expo-linking` - Keep for deep linking

### Likely Safe to Remove

| Package | Location | Notes |
|---------|----------|-------|
| `@ai-sdk/provider` | api | May be imported via barrel |
| `drizzle-orm` | api | Check if D1 is using it |
| `pngjs` | api | Check asset generation |
| `superjson` | api | Check tRPC serialization |
| `@gorhom/bottom-sheet` | app | Check UI usage |
| `@react-native-community/slider` | app | Check UI usage |
| `@slopcade/game-bundler` | app | Check if used |
| `munim-bluetooth-peripheral` | app | BLE feature disabled - **DELETE** |
| `react-native-sortable-list` | app | Check if replaced by sortables |
| `react-native-worklets` | app | Check if worklets-core is sufficient |
| `@dnd-kit/*` | root | Should be in packages/ui only |

---

## 3. Unused devDependencies (24)

Most Storybook devDeps are probably needed. Verify Storybook works before removing.

---

## 4. Next Steps

### Phase 1: More File Deletions
1. [ ] Delete `munim-bluetooth-peripheral` from app/package.json (BLE disabled)
2. [ ] Review `shared/src/` directories for complete removal (slopeggle, motion, loader, scripting)
3. [ ] Delete `ui-compare.ts` and `ui-experiment.ts` (experiments)

### Phase 2: Dependency Cleanup
1. [ ] Move `@dnd-kit/*` from root to packages/ui
2. [ ] Remove `munim-bluetooth-peripheral`
3. [ ] Verify and remove unused Storybook deps

### Phase 3: Export Cleanup
```bash
pnpm knip --fix --dry-run  # Preview
pnpm knip --fix            # Apply
```

---

## Commands

```bash
# Re-run analysis
pnpm knip

# Preview auto-fixes
pnpm knip --fix --dry-run

# Apply auto-fixes (unused exports only)
pnpm knip --fix

# Show only specific issue types
pnpm knip --include files
pnpm knip --include dependencies
```
