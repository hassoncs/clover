// ⚠️ AUTO-GENERATED - DO NOT EDIT
// This file is automatically generated from types.ts
// Any changes will be overwritten on next generation
// To modify: update app/lib/godot/types.ts and run pnpm generate:bridge
//
// Generated by: scripts/bridge-codegen.ts
// Source: app/lib/godot/types.ts (GodotBridge + EffectsBridge interfaces)
// Generated: 2026-02-11T20:21:18.482Z

import { vi } from 'vitest';
import type {
  GodotBridge,
  GameDefinition,
  PropertySyncPayload,
  Vec2,
  EntityTransform,
  CollisionEvent,
  SensorEvent,
  SpawnEntityRequest,
  EntitySpawnedEvent,
  RaycastHit,
  DynamicShaderResult,
  EffectsResult,
  EffectsPipelineSnapshot,
  RevoluteJointDef,
  DistanceJointDef,
  PrismaticJointDef,
  WeldJointDef,
  MouseJointDef,
  NormalizedDrawCommand,
  DrawCommand,
} from '../../types.js';
import type { CompiledPlan } from '@slopcade/shared/effects';

export class MockGodotBridge implements GodotBridge {
  applyGraph = vi.fn<(plan: CompiledPlan) => Promise<EffectsResult>>();
  clearGraph = vi.fn<() => Promise<EffectsResult>>();
  updateParams = vi.fn<(passId: string, params: Record<string, unknown>) => Promise<EffectsResult>>();
  start = vi.fn<() => Promise<EffectsResult>>();
  pause = vi.fn<() => Promise<EffectsResult>>();
  resume = vi.fn<() => Promise<EffectsResult>>();
  stop = vi.fn<() => Promise<EffectsResult>>();
  reset = vi.fn<() => Promise<EffectsResult>>();
  snapshot = vi.fn<() => Promise<EffectsResult<EffectsPipelineSnapshot>>>();
  restore = vi.fn<(snapshot: EffectsPipelineSnapshot) => Promise<EffectsResult>>();
  initialize = vi.fn<() => Promise<void>>();
  dispose = vi.fn<() => void>();
  effectsUpdateParams = vi.fn<(passId: string, params: Record<string, number | boolean | string>) => void>();
  drawToActiveBuffer = vi.fn<(entityId: string, commands: NormalizedDrawCommand[]) => void>();
  loadGame = vi.fn<(definition: GameDefinition) => Promise<void>>();
  clearGame = vi.fn<() => void>();
  setupWorld = vi.fn<(world: GameDefinition['world'], background?: GameDefinition['background']) => void>();
  registerPrefabs = vi.fn<(prefabs: GameDefinition['prefabs']) => void>();
  loadEntities = vi.fn<(entities: GameDefinition['entities']) => void>();
  clearEntities = vi.fn<() => void>();
  pausePhysics = vi.fn<() => void>();
  resumePhysics = vi.fn<() => void>();
  setInspectMode = vi.fn<(enabled: boolean) => void>();
  stepPhysics = vi.fn<(frames: number) => Promise<{ ok: boolean; framesAdvanced: number; endFrame: number }>>();
  callRpc = vi.fn<(method: string, params?: unknown) => Promise<any>>();
  spawnEntity = vi.fn<(request: SpawnEntityRequest) => void>();
  destroyEntity = vi.fn<(entityId: string) => void>();
  instantiateFromScene = vi.fn<(scenePath: string, entityId: string, position: Vec2, properties?: Record<string, unknown>) => Promise<{ entityId: string }>>();
  getEntityTransform = vi.fn<(entityId: string) => Promise<EntityTransform | null>>();
  getAllTransforms = vi.fn<() => Promise<Record<string, EntityTransform>>>();
  setTransform = vi.fn<(entityId: string, x: number, y: number, angle: number) => void>();
  setPosition = vi.fn<(entityId: string, x: number, y: number) => void>();
  setRotation = vi.fn<(entityId: string, angle: number) => void>();
  setScale = vi.fn<(entityId: string, scaleX: number, scaleY: number) => void>();
  setOpacity = vi.fn<(entityId: string, opacity: number) => void>();
  setVisible = vi.fn<(entityId: string, visible: boolean) => void>();
  getLinearVelocity = vi.fn<(entityId: string) => Promise<Vec2 | null>>();
  setLinearVelocity = vi.fn<(entityId: string, velocity: Vec2) => void>();
  getAngularVelocity = vi.fn<(entityId: string) => Promise<number | null>>();
  setAngularVelocity = vi.fn<(entityId: string, velocity: number) => void>();
  applyImpulse = vi.fn<(entityId: string, impulse: Vec2) => void>();
  applyForce = vi.fn<(entityId: string, force: Vec2) => void>();
  applyTorque = vi.fn<(entityId: string, torque: number) => void>();
  createRevoluteJoint = vi.fn<(def: RevoluteJointDef) => number>();
  createDistanceJoint = vi.fn<(def: DistanceJointDef) => number>();
  createPrismaticJoint = vi.fn<(def: PrismaticJointDef) => number>();
  createWeldJoint = vi.fn<(def: WeldJointDef) => number>();
  createMouseJoint = vi.fn<(def: MouseJointDef) => number>();
  createMouseJointAsync = vi.fn<(def: MouseJointDef) => Promise<number>>();
  destroyJoint = vi.fn<(jointId: number) => void>();
  setMotorSpeed = vi.fn<(jointId: number, speed: number) => void>();
  setMouseTarget = vi.fn<(jointId: number, target: Vec2) => void>();
  screenToWorld = vi.fn<(screenX: number, screenY: number) => Promise<Vec2>>();
  queryPoint = vi.fn<(point: Vec2) => Promise<number | null>>();
  queryPointEntity = vi.fn<(point: Vec2) => Promise<string | null>>();
  queryAABB = vi.fn<(min: Vec2, max: Vec2) => Promise<number[]>>();
  raycast = vi.fn<(origin: Vec2, direction: Vec2, maxDistance: number) => Promise<RaycastHit | null>>();
  setUserData = vi.fn<(entityId: string, data: unknown) => void>();
  getUserData = vi.fn<(entityId: string) => Promise<unknown>>();
  getAllEntities = vi.fn<() => Promise<string[]>>();
  onCollision = vi.fn<(callback: (event: CollisionEvent) => void) => () => void>();
  onEntityDestroyed = vi.fn<(callback: (entityId: string) => void) => () => void>();
  onEntitySpawned = vi.fn<(callback: (event: EntitySpawnedEvent) => void) => () => void>();
  onSensorBegin = vi.fn<(callback: (event: SensorEvent) => void) => () => void>();
  onSensorEnd = vi.fn<(callback: (event: SensorEvent) => void) => () => void>();
  onTransformSync = vi.fn<(callback: (transforms: Record<string, EntityTransform>) => void) => () => void>();
  onScore = vi.fn<(callback: (points: number, entityId: string) => void) => () => void>();
  getAllProperties = vi.fn<() => Promise<PropertySyncPayload>>();
  onPropertySync = vi.fn<(callback: (properties: PropertySyncPayload) => void) => () => void>();
  setWatchConfig = vi.fn<(config: unknown) => void>();
  sendInput = vi.fn<(type: 'tap' | 'drag_start' | 'drag_move' | 'drag_end', data: { x: number; y: number; entityId?: string }) => void>();
  onInputEvent = vi.fn<(callback: (type: string, x: number, y: number, entityId: string | null) => void) => () => void>();
  setEntityImage = vi.fn<(entityId: string, url: string, width: number, height: number) => void>();
  setEntityAtlasRegion = vi.fn<(entityId: string, atlasUrl: string, x: number, y: number, w: number, h: number, width: number, height: number) => void>();
  clearTextureCache = vi.fn<(url?: string) => void>();
  createPixelBuffer = vi.fn<(entityId: string, width: number, height: number, clearColor: string, worldWidth?: number, worldHeight?: number) => void>();
  pixelBufferDraw = vi.fn<(entityId: string, commands: DrawCommand[]) => void>();
  pixelBufferClear = vi.fn<(entityId: string, color: string) => void>();
  destroyPixelBuffer = vi.fn<(entityId: string) => void>();
  preloadTextures = vi.fn<(urls: string[], onProgress?: (percent: number, completed: number, failed: number) => void) => Promise<{ completed: number; failed: number }>>();
  setDebugShowShapes = vi.fn<(show: boolean) => void>();
  setDebugSettings = vi.fn<(settings: { showInputDebug: boolean; showPhysicsShapes: boolean; showZones: boolean; showFPS: boolean }) => void>();
  setCameraTarget = vi.fn<(entityId: string | null) => void>();
  setCameraPosition = vi.fn<(x: number, y: number) => void>();
  setCameraZoom = vi.fn<(zoom: number) => void>();
  startCamera = vi.fn<(entityId: string, width?: number, height?: number) => void>();
  stopCamera = vi.fn<() => void>();
  spawnParticle = vi.fn<(type: string, x: number, y: number) => void>();
  playSound = vi.fn<(resourcePath: string) => void>();
  applySpriteEffect = vi.fn<(entityId: string, effectName: string, params?: Record<string, unknown>) => void>();
  updateSpriteEffectParam = vi.fn<(entityId: string, paramName: string, value: unknown) => void>();
  clearSpriteEffect = vi.fn<(entityId: string) => void>();
  setPostEffect = vi.fn<(effectName: string, params?: Record<string, unknown>, layer?: string) => void>();
  updatePostEffectParam = vi.fn<(paramName: string, value: unknown, layer?: string) => void>();
  clearPostEffect = vi.fn<(layer?: string) => void>();
  screenShake = vi.fn<(intensity: number, duration?: number) => void>();
  zoomPunch = vi.fn<(intensity?: number, duration?: number) => void>();
  triggerShockwave = vi.fn<(worldX: number, worldY: number, duration?: number) => void>();
  flashScreen = vi.fn<(color?: [number, number, number, number?], duration?: number) => void>();
  createDynamicShader = vi.fn<(shaderId: string, shaderCode: string) => Promise<DynamicShaderResult>>();
  applyDynamicShader = vi.fn<(entityId: string, shaderId: string, params?: Record<string, unknown>) => void>();
  applyDynamicPostShader = vi.fn<(shaderCode: string, params?: Record<string, unknown>) => void>();
  hotSwapShader = vi.fn<(shaderId: string, source: string) => void>();
  spawnParticlePreset = vi.fn<(presetName: string, worldX: number, worldY: number, params?: Record<string, unknown>) => void>();
  getAvailableEffects = vi.fn<() => Promise<{
    sprite: string[];
    post: string[];
    particles: string[];
  }>>();
  createUIButton = vi.fn<(buttonId: string, normalImageUrl: string, pressedImageUrl: string, x: number, y: number, width: number, height: number) => void>();
  destroyUIButton = vi.fn<(buttonId: string) => void>();
  onUIButtonEvent = vi.fn<(callback: (eventType: 'button_down' | 'button_up' | 'button_pressed', buttonId: string) => void) => () => void>();
  createThemedUIComponent = vi.fn<(componentId: string, componentType: 0 | 1 | 2 | 3 | 4 | 5 | 6, metadataUrl: string, x: number, y: number, width: number, height: number, labelText?: string) => void>();
  destroyThemedUIComponent = vi.fn<(componentId: string) => void>();
  show3DModel = vi.fn<(path: string) => boolean>();
  show3DModelFromUrl = vi.fn<(url: string) => void>();
  set3DViewportPosition = vi.fn<(x: number, y: number) => void>();
  set3DViewportSize = vi.fn<(width: number, height: number) => void>();
  rotate3DModel = vi.fn<(x: number, y: number, z: number) => void>();
  set3DModelPosition = vi.fn<(x: number, y: number, z: number) => void>();
  set3DCameraDistance = vi.fn<(distance: number) => void>();
  set3DCameraSize = vi.fn<(size: number) => void>();
  clear3DModels = vi.fn<() => void>();
  create3DFloor = vi.fn<(size?: number, colorHex?: string, style?: 'plain' | 'grid') => void>();
  create3DCube = vi.fn<(x: number, y: number, z: number, size?: number, colorHex?: string) => void>();
  clear3DCubes = vi.fn<() => void>();
  set3DCameraPosition = vi.fn<(x: number, y: number, z: number) => void>();
  set3DCameraLookAt = vi.fn<(x: number, y: number, z: number) => void>();
  setOrbitControls = vi.fn<(enabled: boolean) => void>();
  setExternalInput = vi.fn<(name: string, imageData: string) => void>();
  setScreenInput = vi.fn<(enable: boolean) => void>();
}
