// ⚠️ AUTO-GENERATED - DO NOT EDIT
// This file is automatically generated from types.ts
// Any changes will be overwritten on next generation
// To modify: update app/lib/godot/types.ts and run pnpm generate:bridge
//
// Generated by: scripts/bridge-codegen.ts
// Source: app/lib/godot/types.ts (GodotBridge + EffectsBridge interfaces)
// Generated: source:d27fbf0251bc

import type { DistanceJointDef, DrawCommand, DynamicShaderResult, EffectsPipelineSnapshot, EffectsResult, EntityTransform, GameDefinition, GameRule, GodotBridge, MouseJointDef, NormalizedDrawCommand, PrismaticJointDef, PropertySyncPayload, RaycastHit, RevoluteJointDef, SpawnEntityRequest, Vec2, WeldJointDef } from '../types';
import type { CompiledPlan } from '@slopcade/shared/effects';
import { normalizeEffectsResult, normalizeEffectsSnapshot, createEffectsSnapshotPayload } from '../GodotBridgeBase';

type GetBridgeFn = () => Window['GodotBridge'] | null;
type QueryAsyncFn = <T>(method: string, args?: unknown[], timeoutMs?: number) => Promise<T>;
type ExecuteEffectsFn = <T = void>(
  method: string,
  params?: Record<string, unknown>,
  mapData?: (rawData: unknown) => T,
) => Promise<EffectsResult<T>>;

export function createGeneratedMethods(
  getBridge: GetBridgeFn,
  queryAsync: QueryAsyncFn,
  executeEffects: ExecuteEffectsFn,
): Partial<GodotBridge> {
  return {
  async applyGraph(plan: CompiledPlan): Promise<EffectsResult> {
    return executeEffects("effects.applyGraph", { plan });
  },

  async clearGraph(): Promise<EffectsResult> {
    return executeEffects("effects.clearGraph");
  },

  async updateParams(passId: string, params: Record<string, unknown>): Promise<EffectsResult> {
    return executeEffects("effects.updateParams", { passId, params });
  },

  async start(): Promise<EffectsResult> {
    return executeEffects("effects.start");
  },

  async pause(): Promise<EffectsResult> {
    return executeEffects("effects.pause");
  },

  async resume(): Promise<EffectsResult> {
    return executeEffects("effects.resume");
  },

  async stop(): Promise<EffectsResult> {
    return executeEffects("effects.stop");
  },

  async reset(): Promise<EffectsResult> {
    return executeEffects("effects.reset");
  },

  async snapshot(): Promise<EffectsResult<EffectsPipelineSnapshot>> {
    return executeEffects<EffectsPipelineSnapshot>("effects.snapshot", undefined, normalizeEffectsSnapshot);
  },

  async restore(snapshot: EffectsPipelineSnapshot): Promise<EffectsResult> {
    return executeEffects("effects.restore", { snapshot: createEffectsSnapshotPayload(snapshot) });
  },

  effectsUpdateParams(passId: string, params: Record<string, number | boolean | string>): void {
    getBridge()?.query?.("effects_updateParams", "effects.updateParams", JSON.stringify({ passId, params }));
  },

  drawToActiveBuffer(entityId: string, commands: NormalizedDrawCommand[]): void {
    getBridge()?.drawToActiveBuffer(entityId, JSON.stringify(commands));
  },

  async loadGame(definition: GameDefinition): Promise<void> {
    return queryAsync<void>("loadGameJson", [JSON.stringify(definition)]);
  },

  clearGame(): void {
    getBridge()?.clearGame();
  },

  setupWorld(world: GameDefinition["world"], background?: GameDefinition["background"]): void {
    getBridge()?.setupWorld(world?.gravity?.x ?? 0, world?.gravity?.y ?? 0, world?.pixelsPerMeter ?? 0, world?.bounds?.width ?? 0, world?.bounds?.height ?? 0, JSON.stringify(background));
  },

  registerPrefabs(prefabs: GameDefinition["prefabs"]): void {
    getBridge()?.registerPrefabs(JSON.stringify(prefabs));
  },

  loadEntities(entities: GameDefinition["entities"]): void {
    getBridge()?.loadEntities(JSON.stringify(entities));
  },

  clearEntities(): void {
    getBridge()?.clearEntities();
  },

  loadRules(rules: GameRule[]): void {
    getBridge()?.loadRules(JSON.stringify(rules));
  },

  async loadScript(source: string): Promise<{ ok: boolean; error?: string }> {
    return queryAsync<{ ok: boolean; error?: string }>("loadScript", [source]);
  },

  pausePhysics(): void {
    getBridge()?.pausePhysics();
  },

  resumePhysics(): void {
    getBridge()?.resumePhysics();
  },

  setInspectMode(enabled: boolean): void {
    getBridge()?.setInspectMode(enabled);
  },

  async stepPhysics(frames: number): Promise<{ ok: boolean; framesAdvanced: number; endFrame: number }> {
    return queryAsync<{ ok: boolean; framesAdvanced: number; endFrame: number }>("step", [frames]);
  },

  async callRpc(method: string, params?: unknown): Promise<any> {
    return await queryAsync<any>("callRpc", [method, JSON.stringify(params)]) ?? undefined;
  },

  spawnEntity(request: SpawnEntityRequest): void {
    getBridge()?.spawnEntity(request?.entityId ?? "", request?.prefabId ?? "", request?.position?.x ?? 0, request?.position?.y ?? 0, request?.velocity?.x ?? 0, request?.velocity?.y ?? 0);
  },

  destroyEntity(entityId: string): void {
    getBridge()?.destroyEntity(entityId);
  },

  async instantiateFromScene(scenePath: string, entityId: string, position: Vec2, properties?: Record<string, unknown>): Promise<{ entityId: string }> {
    return queryAsync<{ entityId: string }>("instantiateFromScene", [scenePath, entityId, position?.x ?? 0, position?.y ?? 0, JSON.stringify(properties)]);
  },

  async getEntityTransform(entityId: string): Promise<EntityTransform | null> {
    return await queryAsync<EntityTransform | null>("getEntityTransform", [entityId]) ?? null;
  },

  async getAllTransforms(): Promise<Record<string, EntityTransform>> {
    return queryAsync<Record<string, EntityTransform>>("getAllTransforms", []);
  },

  setTransform(entityId: string, x: number, y: number, angle: number): void {
    getBridge()?.setTransform(entityId, x, y, angle);
  },

  setPosition(entityId: string, x: number, y: number): void {
    getBridge()?.setPosition(entityId, x, y);
  },

  setRotation(entityId: string, angle: number): void {
    getBridge()?.setRotation(entityId, angle);
  },

  setScale(entityId: string, scaleX: number, scaleY: number): void {
    getBridge()?.setScale(entityId, scaleX, scaleY);
  },

  setOpacity(entityId: string, opacity: number): void {
    getBridge()?.setOpacity(entityId, opacity);
  },

  setVisible(entityId: string, visible: boolean): void {
    getBridge()?.setVisible(entityId, visible);
  },

  async getLinearVelocity(entityId: string): Promise<Vec2 | null> {
    return await queryAsync<Vec2 | null>("getLinearVelocity", [entityId]) ?? null;
  },

  setLinearVelocity(entityId: string, velocity: Vec2): void {
    getBridge()?.setLinearVelocity(entityId, velocity?.x ?? 0, velocity?.y ?? 0);
  },

  async getAngularVelocity(entityId: string): Promise<number | null> {
    return await queryAsync<number | null>("getAngularVelocity", [entityId]) ?? null;
  },

  setAngularVelocity(entityId: string, velocity: number): void {
    getBridge()?.setAngularVelocity(entityId, velocity);
  },

  applyImpulse(entityId: string, impulse: Vec2): void {
    getBridge()?.applyImpulse(entityId, impulse?.x ?? 0, impulse?.y ?? 0);
  },

  applyForce(entityId: string, force: Vec2): void {
    getBridge()?.applyForce(entityId, force?.x ?? 0, force?.y ?? 0);
  },

  applyTorque(entityId: string, torque: number): void {
    getBridge()?.applyTorque(entityId, torque);
  },

  createRevoluteJoint(def: RevoluteJointDef): number {
    return getBridge()?.createRevoluteJoint(def?.type ?? "", def?.bodyA ?? "", def?.bodyB ?? "", def?.anchor?.x ?? 0, def?.anchor?.y ?? 0, def?.enableLimit ?? false, def?.lowerAngle ?? 0, def?.upperAngle ?? 0, def?.enableMotor ?? false, def?.motorSpeed ?? 0, def?.maxMotorTorque ?? 0) ?? -1;
  },

  createDistanceJoint(def: DistanceJointDef): number {
    return getBridge()?.createDistanceJoint(def?.type ?? "", def?.bodyA ?? "", def?.bodyB ?? "", def?.anchorA?.x ?? 0, def?.anchorA?.y ?? 0, def?.anchorB?.x ?? 0, def?.anchorB?.y ?? 0, def?.length ?? 0, def?.stiffness ?? 0, def?.damping ?? 0) ?? -1;
  },

  createPrismaticJoint(def: PrismaticJointDef): number {
    return getBridge()?.createPrismaticJoint(def?.type ?? "", def?.bodyA ?? "", def?.bodyB ?? "", def?.anchor?.x ?? 0, def?.anchor?.y ?? 0, def?.axis?.x ?? 0, def?.axis?.y ?? 0, def?.enableLimit ?? false, def?.lowerTranslation ?? 0, def?.upperTranslation ?? 0, def?.enableMotor ?? false, def?.motorSpeed ?? 0, def?.maxMotorForce ?? 0) ?? -1;
  },

  createWeldJoint(def: WeldJointDef): number {
    return getBridge()?.createWeldJoint(def?.type ?? "", def?.bodyA ?? "", def?.bodyB ?? "", def?.anchor?.x ?? 0, def?.anchor?.y ?? 0, def?.stiffness ?? 0, def?.damping ?? 0) ?? -1;
  },

  createMouseJoint(def: MouseJointDef): number {
    return getBridge()?.createMouseJoint(def?.type ?? "", def?.body ?? "", def?.target?.x ?? 0, def?.target?.y ?? 0, def?.maxForce ?? 0, def?.stiffness ?? 0, def?.damping ?? 0) ?? -1;
  },

  async createMouseJointAsync(def: MouseJointDef): Promise<number> {
    return await queryAsync<number>("createMouseJointAsync", [def?.type ?? "", def?.body ?? "", def?.target?.x ?? 0, def?.target?.y ?? 0, def?.maxForce ?? 0, def?.stiffness ?? 0, def?.damping ?? 0]) ?? 0;
  },

  destroyJoint(jointId: number): void {
    getBridge()?.destroyJoint(jointId);
  },

  setMotorSpeed(jointId: number, speed: number): void {
    getBridge()?.setMotorSpeed(jointId, speed);
  },

  setMouseTarget(jointId: number, target: Vec2): void {
    getBridge()?.setMouseTarget(jointId, target?.x ?? 0, target?.y ?? 0);
  },

  async screenToWorld(screenX: number, screenY: number): Promise<Vec2> {
    return queryAsync<Vec2>("screenToWorld", [screenX, screenY]);
  },

  async queryPoint(point: Vec2): Promise<number | null> {
    return await queryAsync<number | null>("queryPoint", [point?.x ?? 0, point?.y ?? 0]) ?? null;
  },

  async queryPointEntity(point: Vec2): Promise<string | null> {
    return await queryAsync<string | null>("queryPointEntity", [point?.x ?? 0, point?.y ?? 0]) ?? null;
  },

  async queryAABB(min: Vec2, max: Vec2): Promise<number[]> {
    return queryAsync<number[]>("queryAabb", [min?.x ?? 0, min?.y ?? 0, max?.x ?? 0, max?.y ?? 0]);
  },

  async raycast(origin: Vec2, direction: Vec2, maxDistance: number): Promise<RaycastHit | null> {
    return await queryAsync<RaycastHit | null>("raycast", [origin?.x ?? 0, origin?.y ?? 0, direction?.x ?? 0, direction?.y ?? 0, maxDistance]) ?? null;
  },

  setUserData(entityId: string, data: unknown): void {
    getBridge()?.setUserData(entityId, JSON.stringify(data));
  },

  async getUserData(entityId: string): Promise<unknown> {
    return queryAsync<unknown>("getUserData", [entityId]);
  },

  async getAllEntities(): Promise<string[]> {
    return queryAsync<string[]>("getAllEntities", []);
  },

  async getAllProperties(): Promise<PropertySyncPayload> {
    return queryAsync<PropertySyncPayload>("getAllProperties", []);
  },

  setWatchConfig(config: unknown): void {
    getBridge()?.setWatchConfig(JSON.stringify(config));
  },

  sendInput(type: "tap" | "drag_start" | "drag_move" | "drag_end", data: { x: number; y: number; entityId?: string }): void {
    getBridge()?.sendInput(type, data?.x ?? 0, data?.y ?? 0, data?.entityId ?? "");
  },

  setEntityImage(entityId: string, url: string, width: number, height: number): void {
    getBridge()?.setEntityImage(entityId, url, width, height);
  },

  setEntityAtlasRegion(entityId: string, atlasUrl: string, x: number, y: number, w: number, h: number, width: number, height: number): void {
    getBridge()?.setEntityAtlasRegion(entityId, atlasUrl, x, y, w, h, width, height);
  },

  clearTextureCache(url?: string): void {
    getBridge()?.clearTextureCache(url);
  },

  createPixelBuffer(entityId: string, width: number, height: number, clearColor: string, worldWidth?: number, worldHeight?: number): void {
    getBridge()?.createPixelBuffer(entityId, width, height, clearColor, worldWidth, worldHeight);
  },

  pixelBufferDraw(entityId: string, commands: DrawCommand[]): void {
    getBridge()?.pixelBufferDraw(entityId, JSON.stringify(commands));
  },

  pixelBufferClear(entityId: string, color: string): void {
    getBridge()?.pixelBufferClear(entityId, color);
  },

  destroyPixelBuffer(entityId: string): void {
    getBridge()?.destroyPixelBuffer(entityId);
  },

  async preloadTextures(urls: string[], onProgress?: (percent: number, completed: number, failed: number) => void): Promise<{ completed: number; failed: number }> {
    return queryAsync<{ completed: number; failed: number }>("preloadTextures", [JSON.stringify(urls)]);
  },

  setDebugShowShapes(show: boolean): void {
    getBridge()?.setDebugShowShapes(show);
  },

  setDebugSettings(settings: {
		showInputDebug: boolean;
		showPhysicsShapes: boolean;
		showZones: boolean;
		showFPS: boolean;
	}): void {
    getBridge()?.setDebugSettings(settings?.showInputDebug ?? false, settings?.showPhysicsShapes ?? false, settings?.showZones ?? false, settings?.showFPS ?? false);
  },

  setCameraTarget(entityId: string | null): void {
    getBridge()?.setCameraTarget(entityId);
  },

  setCameraPosition(x: number, y: number): void {
    getBridge()?.setCameraPosition(x, y);
  },

  setCameraZoom(zoom: number): void {
    getBridge()?.setCameraZoom(zoom);
  },

  startCamera(entityId: string, width?: number, height?: number): void {
    getBridge()?.startCamera(entityId, width, height);
  },

  stopCamera(): void {
    getBridge()?.stopCamera();
  },

  spawnParticle(type: string, x: number, y: number): void {
    getBridge()?.spawnParticle(type, x, y);
  },

  playSound(resourcePath: string, volume?: number, pitch?: number): void {
    getBridge()?.playSound(resourcePath, volume, pitch);
  },

  playMusic(resourcePath: string, volume?: number, loop?: boolean): void {
    getBridge()?.playMusic(resourcePath, volume, loop);
  },

  stopMusic(): void {
    getBridge()?.stopMusic();
  },

  applySpriteEffect(entityId: string, effectName: string, params?: Record<string, unknown>): void {
    getBridge()?.applySpriteEffect(entityId, effectName, JSON.stringify(params));
  },

  updateSpriteEffectParam(entityId: string, paramName: string, value: unknown): void {
    getBridge()?.updateSpriteEffectParam(entityId, paramName, JSON.stringify(value));
  },

  clearSpriteEffect(entityId: string): void {
    getBridge()?.clearSpriteEffect(entityId);
  },

  setPostEffect(effectName: string, params?: Record<string, unknown>, layer?: string): void {
    getBridge()?.setPostEffect(effectName, JSON.stringify(params), layer);
  },

  updatePostEffectParam(paramName: string, value: unknown, layer?: string): void {
    getBridge()?.updatePostEffectParam(paramName, JSON.stringify(value), layer);
  },

  clearPostEffect(layer?: string): void {
    getBridge()?.clearPostEffect(layer);
  },

  screenShake(intensity: number, duration?: number): void {
    getBridge()?.screenShake(intensity, duration);
  },

  zoomPunch(intensity?: number, duration?: number): void {
    getBridge()?.zoomPunch(intensity, duration);
  },

  triggerShockwave(worldX: number, worldY: number, duration?: number): void {
    getBridge()?.triggerShockwave(worldX, worldY, duration);
  },

  flashScreen(color?: [number, number, number, number?], duration?: number): void {
    getBridge()?.flashScreen(JSON.stringify(color), duration);
  },

  async createDynamicShader(shaderId: string, shaderCode: string): Promise<DynamicShaderResult> {
    return queryAsync<DynamicShaderResult>("createDynamicShader", [shaderId, shaderCode]);
  },

  applyDynamicShader(entityId: string, shaderId: string, params?: Record<string, unknown>): void {
    getBridge()?.applyDynamicShaderToEntity(entityId, shaderId, JSON.stringify(params));
  },

  applyDynamicPostShader(shaderCode: string, params?: Record<string, unknown>): void {
    getBridge()?.applyDynamicPostShader(shaderCode, JSON.stringify(params));
  },

  hotSwapShader(shaderId: string, source: string): void {
    getBridge()?.hotSwapShader(shaderId, source);
  },

  spawnParticlePreset(presetName: string, worldX: number, worldY: number, params?: Record<string, unknown>): void {
    getBridge()?.spawnParticlePreset(presetName, worldX, worldY, JSON.stringify(params));
  },

  async getAvailableEffects(): Promise<{
		sprite: string[];
		post: string[];
		particles: string[];
	}> {
    return queryAsync<{
		sprite: string[];
		post: string[];
		particles: string[];
	}>("getAvailableEffects", []);
  },

  createUIButton(buttonId: string, normalImageUrl: string, pressedImageUrl: string, x: number, y: number, width: number, height: number): void {
    getBridge()?.createUiButton(buttonId, normalImageUrl, pressedImageUrl, x, y, width, height);
  },

  destroyUIButton(buttonId: string): void {
    getBridge()?.destroyUiButton(buttonId);
  },

  createThemedUIComponent(componentId: string, componentType: 0 | 1 | 2 | 3 | 4 | 5 | 6, metadataUrl: string, x: number, y: number, width: number, height: number, labelText?: string): void {
    getBridge()?.createThemedUiComponent(componentId, componentType, metadataUrl, x, y, width, height, labelText);
  },

  destroyThemedUIComponent(componentId: string): void {
    getBridge()?.destroyThemedUiComponent(componentId);
  },

  show3DModel(path: string): boolean {
    return getBridge()?.show3DModel(path) ?? false;
  },

  show3DModelFromUrl(url: string): void {
    getBridge()?.show3DModelFromUrl(url);
  },

  set3DViewportPosition(x: number, y: number): void {
    getBridge()?.set3DViewportPosition(x, y);
  },

  set3DViewportSize(width: number, height: number): void {
    getBridge()?.set3DViewportSize(width, height);
  },

  rotate3DModel(x: number, y: number, z: number): void {
    getBridge()?.rotate3DModel(x, y, z);
  },

  set3DModelPosition(x: number, y: number, z: number): void {
    getBridge()?.set3DModelPosition(x, y, z);
  },

  set3DCameraDistance(distance: number): void {
    getBridge()?.set3DCameraDistance(distance);
  },

  set3DCameraSize(size: number): void {
    getBridge()?.set3DCameraSize(size);
  },

  clear3DModels(): void {
    getBridge()?.clear3DModels();
  },

  create3DFloor(size?: number, colorHex?: string, style?: "plain" | "grid"): void {
    getBridge()?.create3DFloor(size, colorHex, style);
  },

  create3DCube(x: number, y: number, z: number, size?: number, colorHex?: string): void {
    getBridge()?.create3DCube(x, y, z, size, colorHex);
  },

  clear3DCubes(): void {
    getBridge()?.clear3DCubes();
  },

  set3DCameraPosition(x: number, y: number, z: number): void {
    getBridge()?.set3DCameraPosition(x, y, z);
  },

  set3DCameraLookAt(x: number, y: number, z: number): void {
    getBridge()?.set3DCameraLookAt(x, y, z);
  },

  setOrbitControls(enabled: boolean): void {
    getBridge()?.setOrbitControls(enabled);
  },

  setExternalInput(name: string, imageData: string): void {
    getBridge()?.setExternalInput(name, imageData);
  },

  setScreenInput(enable: boolean): void {
    getBridge()?.setScreenInput(enable);
  },
  };
}
