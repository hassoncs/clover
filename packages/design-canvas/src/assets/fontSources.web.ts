// On web, use URI-based font sources pointing to /fonts/ in the public directory.
// This avoids the Metro asset-number → getAssetByID path that crashes in Skia web.
export const FREDOKA_REGULAR = { uri: "/fonts/Fredoka-Regular.ttf" };
