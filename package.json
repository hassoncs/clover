{
  "name": "skia-canvas-wasm-deploy-test-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "app"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "start": "turbo run start",
    "web": "turbo run web",
    "ios": "turbo run ios",
    "android": "turbo run android",
    "clean": "turbo run clean",
    "deploy": "turbo run deploy",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  }
}
