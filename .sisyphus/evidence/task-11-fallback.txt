T11 Audio Fallback Implementation Evidence
==========================================
Date: 2026-02-26

CHANGES MADE
------------
1. api/src/party/content/audio/r2-keys.ts
   - Added LEGACY_AUDIO_CONTENT_TYPE_ALIASES constant: { wager: "estimation", history: "estimation" }
   - Added buildContentAudioR2KeyCandidates(brand, contentType, contentId): string[]
     Returns [canonical, ...legacyAliases] — canonical key first, then all keys whose
     LEGACY_AUDIO_CONTENT_TYPE_ALIASES entry maps to the same canonical type.
     Example for contentType="estimation":
       ["audio/voice/amen/content/estimation/id.mp3",
        "audio/voice/amen/content/wager/id.mp3",
        "audio/voice/amen/content/history/id.mp3"]

2. api/src/party/content/audio/index.ts
   - Exported buildContentAudioR2KeyCandidates and LEGACY_AUDIO_CONTENT_TYPE_ALIASES

3. api/src/trpc/routes/party-content.ts
   - Removed local duplicate buildAudioR2Key() function (was identical to buildContentAudioR2Key)
   - Replaced 2 write-path usages with imported buildContentAudioR2Key (lines 547, 730)
   - Updated linkAudioAssets route (R2 head check, previously line 2417) to use
     buildContentAudioR2KeyCandidates with fallback loop — tries canonical key first,
     then legacy wager/history keys; uses first key found in R2 for asset linking.

TYPE CHECK RESULT
-----------------
pnpm build:types: ZERO errors in modified files
  - api/src/party/content/audio/r2-keys.ts: 0 errors
  - api/src/party/content/audio/index.ts: 0 errors
  - api/src/trpc/routes/party-content.ts: 0 errors
  (Pre-existing errors in registry.ts unrelated to this task: missing gitignored
   definition.json build artifacts — not introduced by this change.)

FALLBACK BEHAVIOR
-----------------
For contentType="estimation" in linkAudioAssets:
  1. Try audio/voice/{brand}/content/estimation/{id}.mp3 → found? use it
  2. Try audio/voice/{brand}/content/wager/{id}.mp3 → found? use it
  3. Try audio/voice/{brand}/content/history/{id}.mp3 → found? use it
  4. All missing → increment notInR2 counter (unchanged behavior)

NO BREAKING CHANGES: Assets previously accessible via stored r2_key in
party_content_assets still work unchanged. The fallback only affects the
linkAudioAssets admin route that scans unlinked content and links audio.
