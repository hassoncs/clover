Task: T8 - Rollback Verification (local D1)
Date: 2026-02-26
Worktree: /Users/hassoncs/Workspaces/Personal/slopcade/.worktrees/opencode-fleet-wrk_025d3e83-template-content-name-unificat

Rollback file reviewed:
- api/migrations/20260226_normalize_content_types_rollback.sql

Verification approach:
- Did NOT execute rollback against local DB to preserve canonical post-migration state required for T8 validation.
- Per task allowance, rollback SQL was reviewed for semantic correctness and validation query was executed.

Rollback pre-validation query from rollback SQL:
Command:
npx wrangler d1 execute slopcade-db --local --command "SELECT json_extract(metadata, '$.original_content_type') AS original_type, COUNT(*) AS cnt FROM party_content WHERE json_extract(metadata, '$.original_content_type') IN ('wager','history') GROUP BY original_type ORDER BY original_type"
Output:
- success=true
- results=[]

Interpretation:
- No rollback marker rows exist in local DB because local dataset has zero party_content rows.
- This is expected for this local environment.

Semantic review of rollback SQL:
- Recreates table with CHECK constraint that includes legacy values ('wager', 'history').
- Restores content_type from metadata $.original_content_type when present.
- Cleans rollback marker via json_remove(metadata, '$.original_content_type').
- Recreates expected indexes after table swap.

Result:
- Rollback migration logic is semantically correct for restoring legacy aliases during compatibility-window rollback.
- In this local DB, rollback behavior cannot be data-observed due to zero-row dataset; logic remains valid for production data with markers.
