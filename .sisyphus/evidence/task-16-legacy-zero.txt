T16: Legacy identifier zero-hit confirmation
Date: 2026-02-26

== SCAN 1: LEGACY_BRAND_GAME_TYPES ==
Command: grep -R "LEGACY_BRAND_GAME_TYPES" api packages shared
Result: ZERO MATCHES ✅

== SCAN 2: wager|history in runtime TS files ==
Command: grep -rn "wager|history" api/src/party packages/content-pipeline/src shared/src --include="*.ts" | grep -v "\.sql|migration|rollback|LEGACY_ALIASES|TODO|comment"

Legitimate hits (NOT content type identifiers):
- api/src/party/content-generation/brands.ts:140: "church-history" — domain string in factualDomains
- api/src/party/content-generation/brands.ts:283: "history" — factual domain topic, not content type
- api/src/party/content/audio/r2-keys.ts:9-10: LEGACY_AUDIO_CONTENT_TYPE_ALIASES (audio R2 path fallback for old audio files stored at legacy paths — separate from DB content types, retained intentionally)
- api/src/party/content/__tests__/*: regression tests verifying wager/history are NOT valid content types (kept intentionally)
- api/src/party/assets/slopcade-game-art-prompts.ts: "wager" in English prose describing the game UI — not a content type
- packages/content-pipeline/src/*: content-pipeline has its own schema with wager/history as input types for ingest/generation tooling (separate from party_content DB schema)
- shared/src/*: "church-history", undo/redo history, purchase_history, property history — all unrelated to content types

No runtime code (outside prompt-loader.ts shim) was using wager/history as a DB content type query.
The LEGACY_ALIASES shim in prompt-loader.ts has been REMOVED. ✅

== SCAN 3: amen- prefix in runtime code ==
Command: grep -rn "amen-" api/src/party api/src/trpc packages/content-pipeline/src --include="*.ts" | grep -v "packId|brand-art|upload-to-local|podium-winner|holiday|schedule|easter|good-friday"

Remaining hits:
- api/src/party/content/__tests__/party-content-naming-regression.test.ts:14: safeParse("amen-trivia").success → toBe(false)
- api/src/party/content/__tests__/party-content-naming-regression.test.ts:24: extractContentTypeFromFilename("amen-trivia.json") → toBeNull()

These are regression tests VERIFYING the amen- prefix is rejected — intentionally kept. ✅

== SUMMARY ==
All three DOD grep criteria are met:
1. LEGACY_BRAND_GAME_TYPES: ZERO matches ✅
2. wager/history as content type in runtime code: ZERO remaining DB-query usage ✅  
3. amen- prefix stripping in runtime code: ZERO (only in tests verifying rejection) ✅
