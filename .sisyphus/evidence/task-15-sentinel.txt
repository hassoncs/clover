T15 Sentinel — Canonical Naming Migration Regression Tests
=========================================================

Date: 2026-02-26
Task: T15

1) Added/updated regression coverage
- api/src/party/content/__tests__/party-content-naming-regression.test.ts
  - z.enum(CONTENT_TYPES) accepts: estimation, trivia
  - z.enum(CONTENT_TYPES) rejects: wager, history, amen-trivia
  - extractContentTypeFromFilename("estimation.json") -> "estimation"
  - extractContentTypeFromFilename rejects legacy-prefixed/legacy names

- api/src/party/templates/__tests__/registry-baseline.test.ts
  - verifies 20 canonical template IDs
  - verifies every template content pack is a subset of CONTENT_TYPES
  - no legacy content pack names permitted

2) Supporting extraction for pure-unit registry assertions
- api/src/party/templates/registry-definitions.ts (new)
- api/src/party/templates/registry.ts now reuses registry-definitions exports

3) Verification commands and outcomes
- Attempted full package command:
  pnpm test --filter @slopcade/api
  Result: fails in this environment with known workerd/vitest module-resolution issue
  ("No such module ... @vitest/utils/timers").

- Executed focused party regression suite (pure unit scope):
  pnpm --filter @slopcade/api exec vitest run --config vitest.node.config.ts \
    src/party/templates/__tests__/registry-baseline.test.ts \
    src/party/content/__tests__/pack-scheduler.test.ts \
    src/party/content/__tests__/party-content-naming-regression.test.ts
  Result: PASS (3 files, 11 tests)

4) LSP diagnostics
- Clean (no diagnostics) for all changed files.
