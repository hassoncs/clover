# Decisions

## 2026-02-18 Phase 1 Skipped
- Keep `@slopcade/` namespace — internal detail, no user visibility
- New apps will be `@slopcade/slopcade-app` and `@slopcade/amen-app` (to avoid collision with existing `slopcade` package name)
