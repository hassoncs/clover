- The `docs/pencil` directory was untracked in the main repo, so it had to be copied into the worktree to be modified.
- The Task 2 worktree did not contain `packages/pencil-core`, `apps/pencil/lib/pencilEmbed.ts`, or `apps/pencil/lib/store-context.ts`, so the protocol extraction had to restore minimal Pencil-owned shells for contracts and runtime/store wiring.
- The failing `apps/pencil` typecheck was primarily blocked by two unrelated local config gaps: Pencil lacked the root `types/glsl.d.ts` include, and its local tRPC helpers relied on `@slopcade/api/trpc` types that are not generated in this worktree, so they had to fall back to `AnyRouter` for Task 2 verification.
- `apps/pencil/lib/pencilEmbed.ts` and `apps/pencil/lib/store-context.ts` had to stay because existing Pencil files already import them (`usePencilBridge`, `usePencilDocumentSync`, `_layout.tsx`, and `app/index.tsx`), so removing them breaks the required `apps/pencil` typecheck even though they are not part of the direct pen-type rewire itself.
- In this worktree the requested Slopcade store adapter file did not exist yet, so Task 3 had to create `apps/pencil/lib/adapters/slopcade-store-adapter.ts` and make `store-context.ts` own the runtime selection of that adapter.
- `apps/pencil` typecheck in this worktree was also blocked by `packages/design-canvas/src/document/useDesignDocument.ts` expecting `migrateDesignDocument` from `@slopcade/protocol/design`, so the protocol design module had to expose that migration helper before the required verifier command could go green.
- Task 4 made the durable Pencil runtime identity the `PencilFileRef` triple (`sessionId`, `projectRoot`, `filePath`), and legacy `gameId` callers now pass through a single translation seam in `apps/pencil/lib/pencilEmbed.ts` before sync or bridge code sees them.
- `pnpm pack` rewrites Pencil workspace dependencies inside the tarballs to plain semver (`1.0.0`), so Slopcade needs root-level `pnpm.overrides` that pin `@pencil/*` back to the local tarballs and pin `@slopcade/theme` back to the surviving workspace package during consumer-repo verification.
