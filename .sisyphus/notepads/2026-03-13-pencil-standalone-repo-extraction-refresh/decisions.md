- Added explicit target standalone repository path (`/Users/hassoncs/Workspaces/personal/pencil`) to all four Pencil architecture docs.
- Explicitly stated that `gameId`, `workspace:${gameId}`, and `chatThreads.*` are compatibility-only and forbidden in new Pencil-owned contracts.
- Created `@slopcade/protocol` as the Pencil-owned source for pen schema parsing and document/store contracts, while leaving `shared/src/types/pen.ts` untouched for non-Pencil consumers in this wave.
- Reintroduced a minimal `@slopcade/pencil-core` compatibility package so the requested `vitest run packages/pencil-core` verification target exists while protocol becomes the actual source of truth.
- Kept the final cleanup focused on import-boundary and typecheck blockers only: the remaining broad file list is almost entirely one-line `@slopcade/shared/types/pen` -> `@slopcade/protocol/pen` rewires required by the verifier's repo-wide grep target.
- Task 3 keeps Pencil runtime code pointed at `@slopcade/pencil-core/contracts` and moves `chatThreads.*` knowledge into a dedicated Slopcade host/store adapter, while `store-context.ts` derives that adapter from the untyped Pencil TRPC client only when a legacy `gameId` runtime is present.
- Task 4 keeps `gameId` and `workspace:${gameId}` compatibility confined to `apps/pencil/lib/pencilEmbed.ts` query-translation plus the existing Slopcade adapter, while `usePencilDocumentSync` and bridge/runtime state now operate on canonical `sessionId + projectRoot + filePath` identity only.
- Task 9 stores reproducible Pencil tarballs under `.sisyphus/vendor/pencil-tarballs/`, excludes the extracted package directories from `pnpm-workspace.yaml`, and rewires active Slopcade consumers (`apps/pencil`, `packages/editor`) to `@pencil/*` tarball specs instead of `workspace:*` dependencies.
