# Secrets are managed via hush — no .env file needed.
# Run dev via devmux (pnpm dev:admin) or directly:
#   npx hush run -t app -- pnpm dev
#
# Hush injects: EXPO_PUBLIC_SUPABASE_URL, EXPO_PUBLIC_SUPABASE_ANON_KEY
# Optional override: VITE_API_URL (defaults to http://localhost:8789)
